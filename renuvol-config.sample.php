<?php
/**
 * Образец конфигурации приёма заявок.
 *
 * ┌──────────────────────────────────────────────────────────────────────┐
 * │  ЭТОТ ФАЙЛ НИКОГДА НЕ ЗАПОЛНЯЮТ.                                     │
 * │                                                                      │
 * │  renuvol-config.sample.php  ← вы здесь. Лежит в git, значения пустые │
 * │  renuvol-config.php         ← рабочий. В .gitignore, только на       │
 * │                               хостинге, вот его и заполняют          │
 * │                                                                      │
 * │  Токен, вписанный сюда, попадёт в публичный репозиторий, и его       │
 * │  придётся отзывать. Это уже случалось дважды.                        │
 * └──────────────────────────────────────────────────────────────────────┘
 *
 * Как пользоваться: скопируйте файл на хостинг под именем renuvol-config.php
 * — либо на уровень выше корня сайта (предпочтительно, тогда он вообще
 * недоступен по HTTP), либо в сам корень, рядом с index.html. api/lead.php
 * ищет его в обоих местах.
 *
 * Альтернатива файлу — переменные окружения RENUVOL_TG_TOKEN,
 * RENUVOL_TG_CHAT, RENUVOL_MAIL_TO.
 */

return [
    // Токен бота от @BotFather.
    'tg_token' => '',

    // ID чата для заявок. Личный ID подскажет @userinfobot;
    // у группы ID выглядит как -1001234567890.
    'tg_chat'  => '',

    // Почта для дубля заявок. Пусто — дубль не отправляется.
    'mail_to'  => '',

    // Необязательно. По умолчанию файлы пишутся во временный каталог сервера.
    // 'leads_file' => '/home/u1234/renuvol-leads.log',
    // 'log_file'   => '/home/u1234/renuvol-lead-errors.log',
];
